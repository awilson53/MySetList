@model MySetList.Models.ChordChart

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ChordChart</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.StoragePath)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StoragePath)
            @Html.ValidationMessageFor(model => model.StoragePath)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ChartID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ChartID)
            @Html.ValidationMessageFor(model => model.ChartID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.OriginalFileName)            
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OriginalFileName)
            @Html.ValidationMessageFor(model => model.OriginalFileName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
